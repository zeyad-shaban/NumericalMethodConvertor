let bisection = (expression, a, b, preErr, maxIttr) => {
    // ahmed hesham part
    let xroot = 0
    alert("bisection not supported cuz developer lazy")
    // fx1 = evaluate(expression, a)
    // in the while loop at end to add to table: addToTable(i + 1, x1, x2, x3, fnx3, err)
    drawFunc(expression)
    return xroot
}